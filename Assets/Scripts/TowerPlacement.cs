using System;
using UnityEngine;

public class TowerPlacement : MonoBehaviour
{
    public Tower selectedTower;
    public bool isPlacing;

    private void Update()
    {
        if (isPlacing)
        {
            
        }
    }
}
